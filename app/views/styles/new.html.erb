<%= render 'shared/page_title', title: 'Create a New Style' %>

<%= render 'shared/errors', obj: @style %>

<div class = "row">
  <div class = "well col-md-8 col-md-offset-2 blend-blue">
    <%= form_for @style do |f| %>

      <%= f.label :name %>
      <%= f.text_field :name %>

     <%= f.submit 'Create New Style', class: 'btn btn-default gradient btn-lg' %>
     <% end %>
  </div>
</div>
