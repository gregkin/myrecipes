<%= render 'shared/page_title', title: @recipe.name %>

<div class = "row">
  <div class = "col-md-4 pull-right center">
    <%= gravatar_for @recipe.chef, size: 200 %>
    <p><h5>Brougnt to you by <%= @recipe.chef.chefname %></h5></p>
  </div>
  <div class = "col-md-8">
    <%= link_to "Edit this Recipe", edit_recipe_path(@recipe), class: 'btn btn-primary gradient pull-right' %>
    <h3><%= @recipe.summary %></h3>
    <div class = "well">
      <p>
        <strong>Steps:</strong>
      </p>
      <%= simple_format(@recipe.description) %>
    </div> 
  </div>
</div>

<h5><%= link_to "Return to the Recipes Listing", recipes_path, class: 'btn btn-primary gradient btn-small' %></h5>